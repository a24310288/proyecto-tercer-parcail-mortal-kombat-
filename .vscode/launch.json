{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Mortal",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}\\mortal.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build (msvc)"
    }
  ]
}
